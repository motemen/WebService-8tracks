NAME
    WebService::8tracks

SYNOPSIS
    use WebService::8tracks;

    my $api = WebService::8tracks->new(
        accesskey => 'your accesskey',
        secretkey => 'your secretkey',
    );
    
    # exploring
    $api->mixes({ sort => 'recent' });

    # listening
    my $session = $api->create_session($mix_id);
    $session->play;
    ...
    $session->next;
    $session->skip;

SEE ALSO
    8tracks Playback API. <http://docs.google.com/View?id=dgm3wh3t_56vmhm7hjc>
